# 血缘是派生视图，不是中台第三对象

把血缘做成主对象，每次引用都要先写血缘行，发布和检索契约又多一种不能被引用的记录。只记从哪来，答错无法追溯被谁用过。资产上的引用计数会漂，回放需要对的是当时的引用，不是一个整数。

血缘在资产详情上拼出来：来源种类（ADR 0025）、版本与对象键与确认字段与审计、以及引用/写回商品/微调导出/考核记录。权威仍在商品、资产和这些记录里。血缘不进检索、不能发布。
