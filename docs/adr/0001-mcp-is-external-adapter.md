# MCP 只给外部 Agent，内部走中台接口

八块能力在同一仓库里，内部读写中台用中台接口；MCP 是该接口面向外部 Agent（Cursor、Claude、以后第九个系统）的适配器，不是模块之间的总线。

对内 MCP 会把协议开销和调试成本加在每条业务路径上，且调用者根本不是外部 LLM 宿主。没有 MCP，又讲不出「提示词 + 工具发现 → 一天接新系统」。因此两套门面、同一份中台接口。
