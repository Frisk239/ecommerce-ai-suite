# 机洗要结构化抽取，允许弃权，禁止编造

规格类问题不能只靠切块；「保质期」「容量」必须是带类型的字段。但把「抽满」当成进入待人洗的门槛，模型会为填 schema 造假，漏抽还会变成一本正经的错值。

机洗按资产种类跑 schema，每个字段可空，找不到就弃权并指向原文。解析失败留在已接入。人洗改字段和正文。该种类的必填字段只在发布时必须齐全，且须操作者确认。客服对规格走已发布结构化字段，开放叙述走切块。
