# 检索索引归中台；客服和连接层共用，禁止各养一套

客服自建向量库、MCP 另搜库表，发布后要两边重建，引用版本会对不上，「中台是权威」只在治理台成立。客服为了延迟持有副本，一旦落后，同一条规格会两套答案。

检索索引是已发布资产的派生视图，不是第三对象。发布时切块入索引；未发布不进。客服问规格先读商品字段，开放叙述走中台检索；MCP 检索调同一个接口。切块算法和拒答阈值是工程标定，不另开索引。
