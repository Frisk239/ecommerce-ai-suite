# 登记必须带字节，禁止空壳

ADR 0003 规定字节走对象存储，但没说能不能先建元数据再补文件。允许空壳，已接入会变成「有资产没内容」，客服和 MCP 一旦误用就穿帮；小文本进库、大文件进盘则变成两套内容。

登记成功 = 已经有一版不可变字节和对象键。文档 / 图片 / 视频 / 素材由上传或生成器写入；对话由回流动作把会话正文写成对象，操作者不必选文件；MCP 登记必须带文本或字节，只丢标题则拒绝。先建空资产再补文件不做。
