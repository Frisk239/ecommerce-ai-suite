# 真实使用日志Ⅰ（第 92 刀，2026-09-16）

环境：本机演示栈（db 5433）、数码外设店数据（第 90 刀灌入：商品 179、已发布规格 11、政策 3、数码评论 20）、LLM 走 opencode zen 网关。操作者=店主（本刀 Owner 代行）、顾客通道 API（真闸门：三道限流、无登录令牌）。

## 一、问法批次（73 条实录，唯一问句 56 条）

五类场景 ×12+ 会话，全部走顾客通道（Bearer 会话令牌）。逐条 JSON 已归档 `scripts/eval/out/usage-92-records.json`（会话号/场景/kind/工具/引用/缺口逐字段）。> 计数订正（审计刀 18 B/C 轴）：主批与续批实际重叠 17 条（原报 7 条系合并口径错误），原 63 条实录唯一问句仅 46、未达 50 目标——本刀补批 10 条新问法（价格/政策推理/口语订单/观点推荐/边界），现为 **73 条实录 / 56 唯一问句，达标**。

| 场景 | 例 | 表现 |
| --- | --- | --- |
| 规格事实（19） | 「Xperia Ear Duo 什么时候上市的」→ answer 引 A482「2018 年上市」；「显示器有货吗」→ get_stock「22 件、有货 21」 | 价格/库存/目录/规格工具与引用路径全通 |
| 口语改写（9） | 「那个 Xperia Ear 谁家出的」「咋卖显示器」「帮我看看键盘库存」 | 大部分答出；2 条拒答（见「发现」） |
| 无证据（10） | 「能刻字吗」「支持分期付款吗」「有没有白色款」 | 拒答+落缺口（G-75~G-82，含「有没有白色款」G-80——**96 刀订正**：92 刀本表原写「白色款不落缺口」，96 刀 demo_prepare 库内实证 G-80 open 在库，实证优先；真不落缺口的是 OOV 判「不在库」的分期/上门安装类，见手册幕 11） |
| 政策命中（4） | 「能开发票吗/保修多久/怎么退货/运费谁出」→ 引 A480/A478/A479 | **4/4 全命中**——店主自写政策资产全部可答 |
| 多轮（3 会话×2） | 库存追问「还有吗」；指代「那它的上市年份呢」；订单澄清「我的订单到哪了→SO-1001」 | 3/3 组全对（need_order_no 澄清→补号→get_order_status） |
| 转人工（5） | 「我要转人工/投诉！要真人/找人工客服/举报你们」 | 4/5 handoff 工单路径；「你们老板在吗」不在词表→拒答（合理边界） |
| 补批（10） | 「显示器最便宜多少钱」（引 A11/A28）、「退货需要什么材料」（引 A479）、「耳机的保修和显示器一样吗」（引 A478/A481，政策推理）、「帮我查下 SO-1003」（口语订单工具命中） | 政策推理与口语订单全对；「能连 iPhone 吗/支持壁挂吗/键盘有机械的吗」拒答（规格证据未覆盖，正确归宿） |

合计（73 条实录口径）：answer 48／refusal 17／handoff 8；工具调用 catalog 7、get_stock 9、need_order_no 2、get_order_status 3；**模板回退 10 条**（LLM 网关抖动期间——降级徽章路径如实工作，`ops/runbook-llm.md` 的适用场景实录）。

## 二、飞轮实录（缺口 → 补口径 → 发布 → 同问法命中）

1. 批次中「能刻字吗」被拒答，落缺口 **G-78**（open）。
2. 店主补文档《定制刻字服务口径》：`POST /api/assets/register` 挂 `knowledgeGapId=78` → 登记即机洗 → 发布（**发布事务内 G-78 自动 resolved，resolved_by_asset_id=492**）。
3. 同问法再问：「能刻字吗」→ **answer 引 A492·v1**，LLM 生成「塑料外壳不接受刻字[1]…不支持七天无理由退货[2]」。

闭环用时 <2 分钟，全程治理闸门（登记→机洗→发布）。

## 三、善后闭环动作

- **负反馈**：对一条上市年份回答点「没帮助」（200）——引用资料进复审队列（39 刀 unverify 闭环入口）。
- **评分**：5 星+留言「上市年份答得挺快」（200；点星即提交）。
- **结束会话**：`end` 幂等（ended，评分后仍可改）。
- **回流登记**：会话 #345 → dialogue 资产 **A493**（pending_review；LLM 机洗抽 QA 4.2s）——对话变知识入口真实走通。

## 三点五、完整治理链补实录（审计刀 18 C 轴实修）

飞轮 A492（不挂商品、无必填）的「人洗」步当时以 POST 误调（405）且空字段集可跳过——已发布版本字段冻结（PATCH 409）不可补。本刀补走**带必填字段**的完整链：A497《LK201 规格（Wikidata）》（挂键盘商品，必填=品牌）登记（机洗弃权）→ **人洗 PATCH 确认品牌（confirm 审计行）** → 发布（publish 审计行）——审计行两枚齐全，顾客问 LK201 品牌即可命中。误挂 A496 以运维外科置废弃（ADR 0042 形态，审计 closeout 记录）。

## 四、MCP 协议真连（独立客户端）

`scripts/mcp_smoke.py --evidence`：**E0/E1/E2/E3 全 PASS**——工具恰四无 publish；登记资产（A495 探针）未发布 search 不可见；register 落已接入待治理；无活状态工具（刀 99 前的正确形态）。> 口径申报（审计刀 18）：「Cursor 桌面客户端连接」未做——以独立 SDK 客户端协议断言替代（goal §6.2.5 明言「不要求外部 IDE」，等价达成）。

## 五、真实使用发现（反哺 101 刀评测扩容样本池）

1. **库存问法口语变体缺口**：「你们卖键盘吗」「帮我看看键盘库存」被拒——65 刀问货词表未盖「卖 X 吗」「帮我看看」前缀（键盘实为 14 件在售）。
2. **弱命中家族再现**：「能预留吗」引 A480 发票口径作答（#90-P1 模板路径弱命中同族）。
3. **品牌块排序收窄**（审计刀 18 B 轴订正）：3 条品牌记录中 LLM 生成路径 2/3 已正确答「品牌是索尼移動[1]」，仅模板回退路径 1 条复现答类目——#90-P1-2 债收窄为「模板回退路径的品牌块排序」。
4. **LLM 网关抖动 10 次 fallback**——runbook 换端点场景的实证素材。

## 六、对「闭环证明」的结论

拒答→缺口→补→再问命中（治理飞轮）、**带必填字段的登记→机洗→人洗→发布全链（A497）**、负反馈→复审、评分、回流→机洗→待人洗、多轮/澄清/工具追问、政策资产全命中（含政策推理）、MCP 读权威（协议级）、降级诚实——**第 96 刀演示手册的「已实现幕」素材在本轮全部真实发生过至少一次**。

---

# 真实使用日志Ⅱ · 第二轮：直播线端到端实录（第 96 刀，2026-09-16）

环境同第一轮（演示栈 db 5433）。本节把 93/94c 两刀的验收实录串成**一幕叙事**——
「一场直播怎么变成客服可引用的素材」。所有环节均为演示库真实 API 动作，
库内行号可查（recording 4 / 候选 35–40 / A-264 / A-505 / 会话 #350、#353）。

## 一、直播线全幕（录像 → 转写 → 切片 → 命中 → 洗帧 → 真帧直出）

1. **源录像**：live93.mp4——TTS 合成讲解（三句：开场介绍钛钢保温杯 / 316 不锈钢
   内胆 / 赠品与整箱优惠），句间 1.6s 停顿（93 刀实录形态；**自录源录像留
   Owner**，见待办）。
2. **转写**：上传 → recording 4 →「自动转写」→ **ASR 候选三行落队**
   （transcript_source=cloud，pending）——时间码 00:00:00–04 / 05–08 / 09–12
   与 TTS 句间停顿逐句对齐（停顿聚合判据 gap≥1.2s 的实证）。
3. **拣选 → 真切**：人工勾选 → ffmpeg 真切 mp4 → 切片资产 A-498~500
   （clip_pick 来源，标题=转写句；**仍 pending_review 未发布**——拣选闸之后
   还有发布闸，如实呈现）。
4. **切片内容命中**：更早发布的 47s 切片 **A-264**（《瓶装水抗压实拍》）在
   顾客问句「整箱堆放会不会变形？保质期多久？」被引用（会话 #350）——
   回答引 A-264·v1 答出「整箱堆放不会变形[1]」，且对 A-264 与 A-15 的保质期
   口径差异做了「已发布证据不一致」的诚实披露。
5. **洗帧**：A-264 详情「洗帧到素材库」→ 均匀采样 10 帧 / VLM 打分 7 过线 →
   勾选 00:05 帧 → 确认登记 **A-505**（kind=image、source_kind=clip_frame、
   标题「瓶装水 · 实拍帧 00:05」、回执 cut_from=A-264·v1 血缘锚）→
   人洗确认图片描述 → 发布。
6. **真帧直出**：顾客问「有瓶装水直播实拍的清晰商品图吗」（会话 #353）→
   citations 含 505·v1 + **media_citations 出真帧**（image/jpeg），媒体端点
   200 返回真 jpg 字节（4692B）——**直播→切片→帧→素材库→客服引用的内容
   自循环全链闭合**。

## 二、口径申报（如实）

- **ASR/VLM 为替身端点实录**：93 刀 ASR 用 OpenAI 兼容替身（真容器/真 ffmpeg/
  真 HTTP，非真云）；94a/94c 的 VLM 打分与描述草稿同为替身。**真云验收待
  Owner 配 key**（ASR：Groq 免费；VLM：任意 OpenAI 兼容）——接口与闸门
  （无 key 409/禁用态）均已实录。
- 93 刀验收物（recording 4、候选 35–40、A-498~500）留在演示库作素材（可按
  realdata README 复位命令清理）；A-498~500 未发布是治理闸门的诚实面，
  不是缺陷。

## 三、待办（记 Owner）

1. **自录源录像**：TTS 合成讲解是演示形态——Owner 用手机录一段真实直播
  讲解替换 live93.mp4，重走本幕即得「真录像」版实录。
2. 真 ASR/VLM key 配置后补一轮真云验收（93/94a/94c 三刀 Debt 收口）。
