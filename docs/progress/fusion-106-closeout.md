# 第 106 刀 closeout：融合形态矩阵实验与实现（A2）

日期：2026-09-17。分支 `feat/fusion-106`（stacked 于 imggen 适配 #154 后）。

## 交付

1. **`scripts/eval/fusion_matrix.py`**：三形态（级联 T×4 / RRF w×3 / 加权 w×4）× ef_search 三档 × affinity before/after 两型 = **661 配置×236 条**，三轮迭代——无闸全形态拒答崩 0 → TAU 封顶仅 56.7%（refusal 向量 top1 cos 0.501–0.754 与正例重叠无阈可分）→ **lexgate**（词法空手才许向量作答，无参数语义闸）后 33/660 保双红线。
2. **定案**：C 加权线性 w=0.5 + TAU=0.60 + lexgate + affinity_before（ef 默认 40 三档零差异）；级联最差/RRF 中游/affinity_after 全线差 2-6pp。
3. **实现**（retrieve() 四出口单点）：词法管线原位 + min-max 归一 + 0.5×cos + TAU 下推 SQL + **NULL 块 fail-open** + 查询向量 LRU(512,维度自检) + 无 key/失败纯词法。
4. **ADR 0058**（三形态数字/两闸发现过程/泛化边界补记/归一几何论证）+ rag-eval-report 第 106 刀节 + 工件。
5. **实现后 vs 107a 基线**：overall@1 88.8→**89.8**、@3 96.6→**97.1**、MRR 0.9248→**0.9320**；conf@1 64→**72**（+2 条）、sem_neg@3 85.7→**92.9**（近邻 6 miss @3 救 2）；**正例 98.8@1 与拒答 86.7 双红线保持**；conf@3 -4.0（conf-007 被挤出，唯一退步格，已归属）。无 key 复跑与基线一致（评审订正措辞：检索位逐位同、噪声面 ±1.7pp）。

## 证据

Owner 门禁 **1585/0/0/0**（+22）+ ruff + **融合后基线亲跑复现**（六分布三指标逐格）；评审亲验无 key 全格一致。

## 评审实修（两轴子代理：无 P0）

- 34/660→33/660 off-by-one 订正（ADR+报告）；ADR 补泛化边界（同集定参无留出集+lexgate 无参数语义闸换语料仍稳）；「逐位一致」措辞精确化（paraphrase 噪声 ±1.7pp 下游效应）。

## 记债

- TAU/w 同集定参泛化边界（换语料/模型须重跑矩阵校准——ADR 已记）。
- conf@3 唯一退步格（conf-007）——101 扩容后语料变可观察。

## 后续

第 107b 刀：消融终表（词法→+同义→+向量三级全指标）+ 第五阶段收官报告。
