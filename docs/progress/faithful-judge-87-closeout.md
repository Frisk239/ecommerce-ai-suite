# 第 87 刀 closeout：faithfulness judge 跑全（评测生成层数字收官）

日期：2026-09-16。`feat/faithful-judge-87`。**无新表无迁移、无新依赖**。

## 背景

goal §6.2 未完成清单的最后一条：「评测 faithfulness judge 列待网关补跑」。两次尝试均半途中断（35 刀 43/79、86 刀 48/80 `LLMUnavailable`），工作区还躺着未提交的半途报告。本刀收官该条 + 顺手清 84 刀记债。

## 交付

| 件 | 交付 |
| --- | --- |
| runner 韧性 | `judge_with_retry`：单条 3 次尝试（间隔 2s）+ 重试尽记未评上并**继续**（fail-soft）；失败条目 id 进报告注记；空 key 仍全局跳过。此前单条 `LLMError` 即**永久终止整列**（`run_eval.py` 旧循环）——中断史的根因。线上 `services/llm` 刻意的 20s/0 重试契约（0018/0033）零改动 |
| judge 首次跑全 | 96 条 / answered 80 **全部评上、零失败条目**（无一条走到重试尽）；检索层六列与 83 刀基线逐位相同（90.0/86.2）；工件 `scripts/eval/out/report-judge-87.md` 入库，86 刀半途报告删除 |
| 定位订正（本刀核心认知） | **overall 忠实度 10.0% 不是幻觉率**——它是「严格逐句原文对照」口径下回答措辞与证据的逐字贴近度。三层证据：①对照实验（同一证据，带框架语回答 False / 剥框架语 True）；②判 supported 的 8 条**全部是对话资产**（模板对 dialogue 是 chunk 原文逐字粘贴、对 document 是字段改写+框架语——判分面按资产种类二分）；③两遍复跑聚合逐位一致（8/80）。真幻觉面在 LLM 生成路径，由线上闸（58/69/81 刀）+ `chat_fallbacks_total` 指标守，不进本可复现评测尺 |
| goal.md 收官订正 | §6.2 未完成清单清零：Wikidata 空 schema（74 刀 92/92 回填）与 judge 列（本刀）移入「已解决项移除」 |
| 84 刀记债清偿 | `ops.fetch_published_refs` 补 `discarded_at IS NULL` 过滤（运维手工 SQL 废弃已发布资产的漏网形态——常规路径有 `_can_publish` 废弃闸，造不出该形态） |

## 验收

- 集成 **1204 passed / 0 skipped**（1200→1204，+4：单测×3 重试恢复/fail-soft/末次不空睡 + ops 集成×1 废弃不进 refs）；ruff 全过。
- **钉子承重双向验证**：ops 钉子回退过滤（stash）跑即红、恢复即绿；单测 3 枚以 `judge_with_retry` 存在性为承重（回退实现即 AttributeError）。
- run_eval 不带 judge 基线逐位吻合（intake 已验）；带 judge 两遍聚合一致。
- 门禁纪律：全量 pytest 显式带 `SUITE_TEST_DATABASE_URL`（79 刀教训：不带则 333 用例静默 skip 假绿）。

## 评审实修（Owner 亲做双轴；子代理两次启动失败[环境 reasoning-level 故障]，按 slice-owner 降级路径亲手全 diff 评审并留记录于此）

- **P1 判 supported 的 8 条未定性**：报告初版对组间差异（paraphrase 12.0 / positive 10.0 / confusion 6.7）写「分布噪音」是超出证据的推断 → 第二遍全量逐条 verdict 定性（全部 dialogue 资产），机制订正为「组间差异 = 三组期望资产 dialogue 占比的投影」；「分布噪音」措辞删除。
- **P2 跨网关漂移边界未声明** → 补「两遍一致仅当日同网关口径，跨日/跨网关未测，引用数字带跑全日期」。
- 其余检查通过：fail-soft 无终止路径（`judge_error` 仅空 key 分支赋值）；monkeypatch 对象与模块内引用方式一致；自建商品防互染论证成立（module 级共享库 + 首用例发布过保温杯素材 + QC「正文必含商品名」）；报告数字与工件逐位一致；diff 无 Must 外改动。

## 认知订正（记录）

1. **「低忠实度数字」先看协议与形态再下结论**：10% 第一眼像「90% 编造」，实际是 judge 严格逐句原文对照 × 模板组装框架语（81 刀特意加的资料名恰是 chunk 里没有的）的错配——与 81 刀「先测量再修，别修错地方」同族。
2. **judge 中断史是产品缺陷不是网关问题**：35/86 两刀都归因「网关不稳定」，真根因是 runner 单点失败设计。网关只要抖一次，整列就死。
3. **评测尺的可复现性分层**：检索层零 LLM 严格可复现（35 刀两遍 diff 为空）；judge 层是 LLM 调用、当日同网关口径下实测稳定、跨网关边界未测——引用时分层说。

## 记债（本刀未吞）

- LLM 生成路径（run_ask 真生成输出）的 faithfulness 评测受输出非确定性约束，不进可复现评测尺——属观察项（若做，形态应为「固定种子问句集 + 多次采样分布报告」）。
- judge prompt 对「前缀+原文粘贴」形态的判定松紧（framework 语短前缀是否该豁免）未调——当前口径已如实定位，不急于改协议。

## 后续

goal §6.2 面试级八条至此**无在案遗留**（本刀收官最后一条）。余下大块：部署面（Owner 暂缓）、发布冲突检测（多源治理最后一块，审计刀 10 有真实病例）、OOV 中间地带保守度（语义裁决）。审计刀节奏：上一次审计刀 16 于第 80 刀后，已过 81–87 七刀，**下一刀按节奏应开审计刀 17（覆盖第 81–87 刀）**。
