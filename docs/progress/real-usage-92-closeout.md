# 第 92 刀 closeout：使用剧本Ⅰ（数码店真实使用第一批）

日期：2026-09-16。分支 `feat/real-usage-92`（stacked 链 …→#128→#129（91 刀）→本刀）。

## 交付

`docs/research/real-usage-log.md`——真实使用日志Ⅰ（「实际使用案例」第一份物证）：

1. **问法批次 73 条实录 / 56 唯一问句**（审计刀 18 订正：原报「63 条/去重 56」合并口径有误——两批重叠 17 条致唯一数仅 46，本刀补批 10 条后达标；记录归档 `scripts/eval/out/usage-92-records.json`）：顾客通道 API 全量真跑（三道限流闸下按节奏建 12+ 会话），五类场景（规格/口语/无证据/政策/多轮/转人工）。统计：answer 42（24 带引用）/ refusal 11（去重）/ handoff 4（去重）；工具 catalog 7、get_stock 9、澄清+订单 4；模板回退 10（网关抖动，降级徽章如实工作）。
2. **飞轮一圈实录**：G-78「能刻字吗」→《定制刻字服务口径》挂缺口登记→发布（事务内缺口 resolved→A492）→**同问法再问命中带引用**（LLM 生成路径）。
3. **善后闭环**：负反馈（进复审队列）、5 星评分+留言、结束会话（幂等）、回流登记 A493（dialogue 资产、机洗 4.2s）。
4. **MCP 协议真连**：smoke --evidence E0–E3 全 PASS。

## 证据

- 全部动作为演示库真实 API 调用，逐条 JSON 存档 `%TEMP%/usage92*.json`（本机工件，不进库；统计数字机械摘取自合并脚本输出）。
- 新落缺口 G-75~G-82（8 条 open，飞轮素材池）；G-78 已收口。

## 偏差（审计刀 18 补申报）

0. **原报数字失实**（见交付 1 订正）与 **MCP「Cursor 真连」以协议级 smoke 替代**（goal §6.2.5 等价口径，此前未申报）；**飞轮 A492 人洗步以 POST 误调（405）且空必填可跳过**——已发布版本不可补（PATCH 409），本刀补走带必填全链 A497（confirm+publish 双审计行，见日志 §三点五）。误挂 A496 运维外科置废弃。

1. **主批脚本在 LLM 网关抖动期一次挂起**（「什么时候降价」问 90s 超时后进程死亡）：ps 误判后启动续跑，与恢复后的主批尾部在「无证据2/转人工」两组重叠 7 条——去重后 56 唯一，重复条目不参与统计口径（统计按去重前 63 计数、refusal/handoff 注明去重值）。
2. 品牌问句（#90-P1-2）与弱命中（#90-P1-1）在真实使用中复现确认——预期内（债已挂 101 刀）。

## 记债（进 101 刀样本池，使用日志 §五）

1. 库存口语变体缺口（「你们卖键盘吗」「帮我看看键盘库存」）——65 刀词表扩展候选。
2. 「能预留吗」弱命中答发票（#90-P1 同族）。
3. 网关抖动 10 次 fallback（runbook 实证素材）。

## 后续

审计刀 18（覆盖第 88–92 刀，按每五刀节奏）→ 第 93 刀 ASR 刀。
