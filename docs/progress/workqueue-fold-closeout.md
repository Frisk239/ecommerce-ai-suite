# 第 59 刀 closeout：工作队列导入货折叠（`feat/workqueue-fold-59`）

日期：2026-09-11。依据：审计刀 8 起记债的「工作队列首屏被导入货淹掉」——审计刀 9/10/11 三次复核仍是 **194 行里 180 条导入**（第 51 刀给了来源筛选，但默认视角与首屏没变）。**无新表无迁移，纯前端**。

## 交付

| 件 | 交付 |
| --- | --- |
| 谓词单一来源 | `workQueue.isImportedSource`：按**来源词**判「数据集导入」（`review_import` / `open_dataset` / `wikidata` / `openfoodfacts` / `wands`），不按标题猜 |
| 折叠组 | 资产表格末尾插入一行折叠头「**数据集导入 N 条**（评论语料 / 开放数据集，非人工登记；点开查看）」，默认收起；展开后逐行与主表同列（同一 `<tbody>`，列宽不失配） |
| 不改变的东西 | **默认 tab/范围不变**（仍是「待人洗 + 工作队列」）、**总数与次序不变**（导入组内保持原相对次序）、没有任何数据被隐藏或删除——列表底部的计数与来源筛选 chips 照旧按全量算 |

## 验收（实测）

1. 默认打开资产页（待人洗 + 工作队列）：表格 **15 行**（14 条人工/系统产生 + 1 行折叠头），不再 194 行铺屏；折叠头写「数据集导入 **180** 条」。
2. 点折叠头展开 → **195 行**（180 条导入按原次序铺出），列与主表一致。
3. 来源筛选 chips 计数不变（评论导入 180 / 切片拣选 6 / 会话回流 5 / 上传 3）；`?source=` 筛选与折叠组共存（筛到导入来源时，折叠头显示筛后的条数）。
4. 门禁：集成 **945 passed / 0 failed / 0 skipped**（后端未改）；ruff 全过；前端 build 绿、lint 7/0。

## 诚实披露

- **没有改默认视角**：「默认是否只显示人工来源」是 Owner 观感裁决（审计刀 8 起挂账）——本刀做的是**把导入货折到队尾**，任何一条都还能看到（点一下展开），不是过滤掉。
- **展开态不持久**（组件内 state，切走再回来收起）——与商品页「其余商品」折叠的既有口径一致，不引入 URL 参数。
- 前端无单测基建：折叠行为靠 build/lint + 真浏览器点穿（默认 15 行 → 展开 195 行）。
- 端到端证据是**本会话实测输出**（界面行数与文案回显），未落成截图文件。

## 后续

- 第 60 刀：**golden 过期期望维护**（审计刀 11 的治理性订正让 `pos-017` 期望的 `13/v3` 过期，基线 67.5% 里的那 1 条即此）——更新期望并复跑，把基线数字恢复到可解释状态。
- 其后按节奏：**审计刀 12**（覆盖第 51–60 刀）。
- 仍待裁决：评分可改、分币种报价。
