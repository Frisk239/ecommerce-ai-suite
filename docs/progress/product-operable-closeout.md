# 工程第 41 刀 closeout：商品可运营+目录可答（feat/product-operable-41）

日期：2026-09-10。上刀：roadmap v3 立项（PR #52/#53）。依据：roadmap v3 第 41 刀行（差距分析最痛#1）；领域裁决：**ADR 0045**（价格字段+目录回落）。基线=`origin/main`（53070b9）。**交接收口**：另一执行者实现主体（门禁全绿+ADR 好+测试正当），Owner 接手修三处词表/工件缺陷+走完流程。

## 交付（roadmap v3 第一刀——商品从只读道具升格为可运营对象）

1. **价格字段**（迁移 0018）：products.price_cents+currency（单店单币种默认 CNY，NULL=未定价）；写回不碰价（价格只在产品档直写，与资产发布写回正交——ADR 0045 核心裁决）；改价记产品档审计（action=product_create/product_update）。
2. **POST/PATCH /api/products**：操作者鉴权；category_schema 模板校验 spec_values；重名拒/bool 挡价/显式 null 回未定价。
3. **目录回落**（`catalog_tools.py`）：retrieve 空命中→目录意图判定（纯列举「卖什么/卖啥/有目录吗…」→列举前 20 件带价；显式价格词→商品匹配报价）→工具式模板回答（citations 恒空——商品行是工具数据源非引用，0002 同口径）；三道闸（真人/政策词/规格词）+零回归（有证据永不回落）；**miss 归宿照旧拒答留缺口——去补=上新/改价，同问可答**（演示实证）。
4. **web**：商品页上架+编辑抽屉（价格直写即时生效+规格只读写回锚）；ProductSelect 搜索组件统一接入三处下拉（登记/素材/编排）。
5. **golden 13→16**（列举/报价/miss+帆布包锚，走真实引擎）；评测大集 96 条零漂移（工件两遍落盘）。

## Owner 交接实修（审查揪出三处）

报价政策词闸+钉测；列举口语词表+语气字符类+钉测（含字面串 vs 字符类 bug）；评测工件落盘（报告数字逐位对上）。

## 计数

- ruff 全过；无 DB `738/550/0/188`；带 DB `738/738/0/0`（688→738 只增）；web build 过；评测 65.0/75.0 逐位零漂移

## 遗留

P2×3（类目常量双写/列举上限硬编码/oxlint 提示）；价格写端点审计动作是新 action 值（血缘 writebacks 不混——钉测在）。

## 下一刀

**第 42 刀转人工真闭环**（roadmap v3：词表+提议双路径/工单号+回执/联系方式捕获/操作者置顶）→ 43 仪表 → 44 退货状态 → **审计刀 8（45 后）**。CONTEXT 推进句已回写。
