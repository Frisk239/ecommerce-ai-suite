# 审计刀 15 closeout：三路并行只读审计第 71–75 刀（PR #104–#108）

日期：2026-09-11。三轴（A 设计符合性 / B 健壮性对抗 / C 产品面 live）。**P0×0（三轴全零）、
P1×2、P2 若干——全部实修/处置。**

## 三轴裁决

| 轴 | 裁决 |
| --- | --- |
| A 设计 | 71–75 契约与代码逐项一致（改评四处一致/last_tool_subject 口径一致/75 零检索改动 git 证实）；迁移链 0029 唯一头；金标 30 例全绿且 eg-mt-003/004 载荷性推理成立；计数链 1157→1158→1161 对账；P2×3（slices 71 条目 1157 应 1158、CONTEXT 待裁决仍列评分可改、71 closeout 漏写验收副作用） |
| B 对抗 | **P1：eval runner 在当前演示库拒跑**——72 刀发布 asset 17 v2 使 golden pos-038 过期（60 刀的 fail-closed 守卫正确拦截，但 72/74 都没复跑 eval：流程「发布类治理动作后核对 golden」被静默违反两刀）；P2×6（csat 指标 docstring 口径、并发首评 409 竞态、created_at 窗口、asset 268 重复引用、材质 3 vs 13 冲突、73 刀死参数/重复调用）；conftest 放宽顾客闸验证安全（限流专测全自建闸） |
| C live | 六项产品主张全部 live 复现（评分三步改、上班时间 v2、多轮复用、Vivo 写回+带引作答、75 零改动、金标对拍 3/3）；**P1：8 条 open 缺口当前检索已可命中**（其中 G25/G30 的 top 命中是无关评论——跨商品混淆病根的实例）；P2×4（评分刷新丢本地态、操作者面无 updated_at、「。。」/《补口径》标题观感、asset 260-262 探针残留） |

## P1 实修（2 条）

1. **golden 维护 + 基线刷新（B）**：pos-038 期望 17/v1→**17/v2**（top-1 仍是 asset 17，60 刀先例）；ref-011「你们营业到几点」被 72 刀的上班时间文档**合法答掉**——按生成协议换同域零命中话题「可以开发票吗」（逐词法验证 0 命中）。**新基线：positive 72.5/77.5、overall 66.2/76.2、拒答 100%、误拒 0.0**——比旧基线（70.0/65.0/2.5）**真改善**，来自 74 刀发布的 Vivo 规格资产（一条 positive 新命中、一条原误拒转正），检索本体零变化（75 刀结论仍立）。已落 rag-eval-report「审计刀 15 订正」节；**流程教训升为约束：任何发布/修订类动作（含演示数据刀）合并前必须复跑 run_eval**。
2. **陈旧缺口处置（C）**：G37（退货运费）/G38（物流咋样）用产品机制关闭（再问即答即收口）；G29（保温时长）**实问被覆盖闸诚实拒答**——留治理（真缺口，C 轴只读预测与 live 覆盖闸的差异如实记录）；G23/G33（刻字）/G2（库存政策，top1 是探针资产 260）/G25/G30（无关评论命中——混淆病根实例）留 open + 记录。

## P2 实修/处置（择要）

- **并发首评竞态（B）**：评分路径改**原子 UPSERT**（INSERT … ON CONFLICT DO UPDATE）——并发两次提交的败者按唯一约束转入改评（覆盖式留最新的自然延伸），不再吐语义过时的 409；live 复验首评 updated_at=None、改评带值。
- **csat 指标口径（B）**：docstring 订正为**事件计数**（含改评）——与仪表分布（读行最新值）不同源，看板别混用。
- **73 刀清理（B）**：`last_tool_subject` 去死参数（exclude_message_id 对 agent 轮查询是 no-op）、报价回落去重复调用。
- **A 的三处文档订正**：slices 71 条目 1158、CONTEXT 评分可改移出待裁决、71 closeout 补写验收副作用（session 198 + 评分行）。
- **留档不动**：asset 268（重复引用，74 已披露）；材质 316 vs 钛钢冲突（72/74 动过的资产面，跨商品混淆病根实例——rerank 观察项）；asset 260-262 探针残留（已发布不可废弃，演示噪音记档）；评分刷新丢本地态/操作者面无 updated_at（记观察项）。
- **环境注记（B）**：`CREATE DATABASE … TEMPLATE suite` 会把 pgvector 容器打崩（宿主文件复制问题）——评审克隆演示库须走 pg_dump/restore；仓库代码不使用 TEMPLATE（非缺陷）。

## 门禁

集成 **1161 passed / 0 failed / 0 skipped**（UPSERT/清理不增不减用例）；ruff 全过；前端未改；
**eval runner 恢复绿：新基线 72.5/66.2**（订正记录见 rag-eval-report）。

## 写副作用（如实）

- C 轴：2 会话（204/237）/22 消息/3 次评分（session_ratings +1 行）/G52 会员积分（open，72 刀披露的自然产物）/H-0046 工单。
- B 轴：演示库零写入（克隆已删；TEMPLATE 崩溃两次由 pg 自动恢复，数据完整核验 269/115/0029）。
- Owner 处置：G37/G38 关闭（1 操作者会话）；UPSERT live 复验 +1 顾客会话 +2 评分提交。

## 后续

- 完善路线 7 项 + 审计刀 15 全部收口。剩余方向：部署面（用户搁置中）、rerank（按评测数字触发的观察项，75 刀实验数字为对照基线）、演示库 260-262 探针残留（需 Owner 决定是否数据手术）。
- 审计刀 16 按节奏于第 80 刀后。
