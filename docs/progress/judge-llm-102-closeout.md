# 第 102 刀 closeout：生成路径忠实度观察（87 刀遗留观察项收官）

日期：2026-09-17。分支 `feat/judge-llm-102`（stacked 于 `feat/eval-expand-101`）。

## 交付

1. **`run_eval.py --judge-llm`**：对真实生成回答跑 judge——存量 216（285 带引用 answer 剔模板指纹 27+去重）+现场真问 10（真 LLM 生成 10/10）=**226 样本全评上**（0 失败，87 刀重试纪律兜住一次网关超时）；复用 judge_with_retry+严格逐句协议。
2. **数字与分层**：supported **69.0%**（存量 68.5/新问 80.0）；70 条 False 三层——**真矛盾 16 条**（评审 P1 拆层：其中「钛钢」×6 属**知识库自矛盾**（A-13/A-3 材质口径打架，病根在数据记债），**净模型幻觉 ≈10 条 4.4%**：品牌=配料、评论泛化成规则、程度自造等）/覆盖否认 32（#90-P1 债 2 的生成面投影）/归属断言 22（87 刀框架语的生成镜像）。
3. **对照 87 刀**：模板 10.0% → 生成 69.0%——框架语病根在生成路径基本消失；不可比声明在案（证据面口径不同）。
4. **定位声明**：一次性观察非可复现尺、不进 CI、当日网关口径。

## 证据

- Owner 门禁 **1495/0/0/0**（+8）+ ruff；评审抽 4 条 False 分层全对（证据原文查库证实）。
- 工件 `scripts/eval/out/report-judge-102.md`。

## 评审实修（两轴子代理：无 P0）

- 「真幻觉 7.1%」拆层降格（KB 自矛盾 6 条归数据债、净幻觉 4.4%）——**面试口径更新：模型净幻觉面 4.4%，另有 KB 自矛盾 2.7% 需治理对齐**；
- prompt 编号泄漏样本（msg1326「A-264/A-15」溢出）进债清单（后续检索闸刀排查）；
- roadmap 单测计数 7→8 订正。

## 记债

1. **A-13/A-3 材质口径打架**（KB 自矛盾）——治理路径开修订对齐（演示数据债）。
2. **prompt 编号泄漏**（标识符溢出到生成面）——候选刀排查。
3. 覆盖否认 32 条=#90-P1 债 2 生成面投影（债已在 101 样本池）。

## 后续

第 103 刀：压测（locust 三路径+限流闸验证；本机口径先跑、服务器实挂后补真环境数字）——**第四阶段最后一刀**。
