# 审计刀 16 closeout：第 76–80 刀三路审计

日期：2026-09-11。`feat/audit-16`。范围：PR #111–#115（76 走查修复二批 / 77 会话搜索 / 78 演示路径 / 79 实体亲和重排 / 80 顾客侧结束会话）。三轴独立子代理（A 设计符合性 / B 技术健壮性 / C 产品面+live）。

## 三轴结论

| 轴 | P0 | P1 | P2 |
| --- | --- | --- | --- |
| A 设计 | 0 | 2（文档层） | 5 |
| B 健壮 | 0 | 1（与 C 重合） | 6 |
| C 产品+live | 2 | 3（1 条已披露残差、1 条与 B 重合） | 4 |

**全数实修/处置**。集成 1174→**1180 passed / 0 skipped**（+6）；ruff 绿；评测逐位不变（87.5/86.2，审计刀未动检索本体）。

## P0 实修（C 轴）

1. **裸覆盖标记退化答案漏出**：模型偶发输出整句只有覆盖类词的形态（live 实测「[未覆盖]」带引用以 kind=answer 漏给顾客，~8% 复现）——`_NO_COVERAGE_RE` 的证据类主语闸拦不到。修法：`strip_coverage_disclaimers` 句级加 `_BARE_COVERAGE_RE` **fullmatch** 判定（整句去引用标记/空白/标点后恰为覆盖词才摘），钉子 5 个（含「配料信息中不包含防腐剂」不误摘的负例——刀 12「误判比漏检贵」口径不变）。
2. **具名 OFF 商品问句不稳定拒答**（「M&M white的条码」6 次 3 答 3 拒）：根因=OFF 导入数据标题与正文品牌错配（M&M white vs Fitpiggy）+ 覆盖闸对厂商措辞敏感，**非 76–80 引入**，治本需评测对照调闸（风险面大）。处置：README 避开清单补「具名 OFF 商品规格问句」条目（SO-1001 口播同款先例）；记债「title 命中下的 no_coverage 误拒」。

## P1 实修

1. **并发回流孤儿资产**（B/C 双轴 live 复现）：`register_asset` 内部 commit 后撞并发 409，旧实现只 rollback——资产行+对象字节已落地成永久孤儿（实测 201+409 双资产+孤儿字节）。修法：409 分支按 **0042 废弃语义补偿收口**（置 discarded_at + 删未发布版本字节，版本行留审计锚）+ warning 日志；钉子 `test_register_race_orphan_asset_is_discarded`（monkeypatch 模拟机洗 commit 后竞争者抢先收口）。演示库的 2 份探针孤儿（271/274）同口径手工废弃。
2. **ADR 0023 幻影态**：`status（active/closed/registered）`——closed 从未实现（0002 docstring 笔误）且漏 ended。订正为三态真实语义。
3. **roadmap CSAT 触发承诺未回写**：第 48/80 刀的「常驻评分条、不做触发式提示」偏离在 roadmap 加订正注记（偏离裁决指向 intake）。
4. **OOV 实体引他品证据**（「雀巢咖啡的配料」引花生酱）：ADR 0048 已披露残差（无标题交集救不动），用户面仍不理想——记债「拒答面覆盖实体不在库分支」，不扩刀。

## P2 择要（全处置）

- `register_session` docstring 自相矛盾（「此处不 commit」vs register_asset 内部 commit）——正是孤儿 P1 的认知根源，订正。
- 操作者发问 409 裸英文状态 → `ServiceSession.STATUS_LABELS`（models 单一来源，顾客/操作者两面共用；customer.py 本地 dict 改引用）。
- slices.md 头部层叠（内嵌过期「下一刀」）收敛 + 76–80 刀 PR 号补齐；CONTEXT「会话终态语义」移出待裁决；walkthrough 方案标 superseded + 列号笔误；README 演示路径计数漂移改定性。
- 亲和 title_terms 重复解析（10k 资产时 ~1.2s/次）：**记债不修**——资产数远小于行上限 10000，当前规模 0.55ms（B 轴自评非当前规模缺陷；ADR 0048 已声明 idf 现算权衡）。
- end 零指标、IP 闸共享、并发测试盲区（部分由孤儿钉子补上）：记债。

## live 复核要点（C 轴）

- 79 刀 rerank 顾客面 8 问抽测：Erdbeeren/钛钢保温杯/Chocolate n3/保温杯材质全部命中问句点名商品；「钛钢保温杯 500ml…」上标 [2] 落 A-0009（次要证据位，主引正确）。
- 80 刀结束会话全路径（幂等/闸/评分/回流）三轴各自独立复现通过。
- 76–78 刀抽查（考核可达性/搜索/README 主线会话可达）全部兑现。

## 数据披露

- 三轴探针自建会话与资产：空会话 7 个已删（demo_reset 同口径）、孤儿资产 2 份已废弃；有消息的探针会话保留（无害演示素材）。
- 演示库现状：会话 148、孤儿 0。

## 后续

- 审计清零状态延续（本刀 P0×2 均为**存量暴露**而非 76–80 新引入：裸标记是 58 刀闸的邻缝、OFF 错配是 50 刀导入数据的形态）。
- 记债清单：title 命中下的 no_coverage 误拒（评测对照）、OOV 实体拒答分支、会话生命周期指标（`service_session_transitions_total`）、亲和 title_terms 缓存（规模触发）、end 独立限流闸。
- 下一刀待 Owner 裁决：探针资产 260-262/268 处置（最后一条挂起裁决项）或部署面（用户先前搁置）。
