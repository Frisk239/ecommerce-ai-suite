# 第 90 刀 closeout：开店数据刀

日期：2026-09-16。分支 `feat/digital-store-90`（stacked 链 #124→#125→#126→本刀）。

## 交付

数码外设专营店上线（用户路径全通：顾客问数码规格/库存/政策均命中带引用）：

1. **类目模板+演示价**：`category_schema.py` 四新类目（品牌 required；显示器+高宽、耳机+上市年份 optional）；`seed.py` 演示价四条（399/199/1499/599 元，mock 口径）。
2. **Wikidata fetch 扩展**：`--digital-only`（QID 实测修正常量 Q250/Q7987/Q5290/Q186819）+ 属性映射（P176→品牌走 label、P571→年份、P2048/2049→米转厘米）+ 逐查询缓存（WDQS 429 退避可续跑）。
3. **规格文档治理发布**：新脚本 `publish_digital_specs.py`——有品牌的耳机/显示器 11 份走「登记→机洗弃权→人洗确认→发布写回」全链（33/74 刀同通道），幂等锚 (title, product_id)，单行失败不拖垮整跑；来源词 wikidata（迁移 0028 拆细口径）。
4. **评论类目筛选**：`load_reviews.py --cats`（默认行为不变）+ 顺带修复 HEAD 上 `--import` 必崩的 `args.db` 未定义潜伏 bug（Spec 轴认定）。
5. **灌入执行（演示库）**：数码商品 +64（总 115→179）；规格资产 11 发布；数码评论 +200（发布 20）；政策文档 3 份发布（upload 来源，店主口径全文在施工单）。
6. **基线刷新对账**：rag-eval-report 追「第 90 刀」节——三段式对账（87 基线 90.0/86.2 → 中途态 85.0/83.8 → after **90.0/86.2、拒答 69.2（13 条组）**）；golden 维护 5 条申报（60 刀先例）；漂移逐位归属。
7. **测试**：`test_realdata_scripts.py` 29→38 例（QID 钉死/换算边界/cats 筛选/规格候选形状）；全量 **1218 passed / 0 failed**（机械摘取）；ruff 过。

## Owner 路径验收（真 API 实跑）

- 「显示器有货吗」→ 库存工具类目聚合（22 件、21 有货）✓
- 「Xperia Ear Duo 上市了吗」→ 引 A482 答上市年份 2018（LLM 生成路径）✓
- 「Xperia Ear 是什么品牌的耳机」→ **3/3 答不出品牌**（品牌值块排证据第 3）——记债 #90-P1

## 偏差（子代理中断与 Owner 接管，如实记录）

1. **实现子代理运行 92 分钟后中断**：根因=WDQS outage 限速（429 按 Retry-After 1000s 退避，五类查询串行即 >1h）。中断时点：评论+政策已灌、fetch 未成、规格/基线未做。Owner 接管补完：fetch 前台重跑成功（64 件）、规格发布 11、基线刷新三段式对账。
2. **before 基线失守**：子代理未按施工单「灌入前先跑 eval」执行（先导评论后中断于 fetch），对账改为「87 基线→中途态→after」三段式，报告如实分段。
3. **Owner 实修**：fixup 来源词 open_dataset→wikidata（迁移 0028 有专属词，子代理用错；print 文案一并订正）；fetch 注释块重复两遍删一处；publish run() 加单行 try/except（OFF 先例形态）；realdata README 六处更新（--digital-only/--cats/--db、新脚本、幂等表、38 例、尾注「已接线」订正）。

## 记债（#90-P1，均已进 rag-eval-report 第 90 刀节，归 101 刀样本池）

1. 模板路径字面弱命中无闸（4 拒答样本+pos-038：问「防水」答「保修」类）。
2. 字段值块排序靠后时品牌问句答不出（Xperia Ear 案例：块序+生成证据利用组合问题）。

## 后续

第 91 刀部署刀（IP+HTTP、ops 文档+runbook、widget 宿主页）；辅轨视频（Pexels key 待申请）在使用剧本Ⅱ前补。
