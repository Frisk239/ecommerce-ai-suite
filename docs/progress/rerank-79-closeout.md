# 第 79 刀 closeout：实体亲和重排（跨商品混淆正解）

日期：2026-09-11。`feat/rerank-79`。**无新表无迁移**（候选 SQL 加拉 `assets.title` 一列）。

## 交付

| 件 | 交付 |
| --- | --- |
| 病根裁决 | 75 刀拒绝来源权重时定位的病根=字段块（品牌：/净含量：…）跨资产同文/近同文，字段名 bigram 分数并列、短块微弱胜出、`(asset_id, chunk)` 稳定出榜。本刀补上缺失的判别信号：**问句与候选资产标题的 IDF 加权 bigram 覆盖**（per-query，纯词法零 LLM 零向量） |
| 实验 | `scripts/eval/rerank_experiment.py`（入库，75 刀流程同款）：乘数 `score×(1+α·aff)` α∈{0.5..4} 与近并列破平 `tie-r*` 矩阵；tie 形态只救同分并列动不了主形态；**α=3 是甜点**（α=4 时 confusion 回落：conf-013 被标题明写「物流速度快」的他品评论抢走） |
| 实现 | `retrieval.py`：`title_idf`/`title_affinity`（纯函数）+ `AFFINITY_ALPHA=3.0`；`retrieve()` 候选 SQL 随带 title，打分后乘 `1+3·aff`——**四出口同语义**（引擎双通道/MCP/缺口验证/评测），无实体问句乘数恒 1 零漂移 |
| ADR | **0048**（背景/决策/数字/取舍：混淆组无实体问句救不动、空标题资产中性、无向量路线不变、α 可独立校准须复跑评测） |

## 评测（96 条大集，before/after）

| 指标 | before（审计刀 15 基线） | after |
| --- | --- | --- |
| positive recall@1 | 72.5% | **87.5%** |
| paraphrase recall@1 | 60.0% | **92.0%** |
| confusion 混淆@1 | 60.0% | **73.3%** |
| overall recall@1 | 66.2% | **86.2%** |
| overall recall@3 | 76.2% | **92.5%** |
| 拒答率 / 误拒率 | 100% / 0.0% | 100% / 0.0% |

**新基线 87.5/86.2**。top-1 变动 23 条=16 改善/2 变差/5 横移；两条变差均为语义歧义非硬伤（pos-008「该商品的退货政策」无指代下 6 与 13 都是政策资产；syn-012「钛钢保温瓶」对 3/9 近邻歧义——syn-020 同款问句期望正是 9），论证见 ADR 0048。

## 验收

- 集成 **1166 passed**（1161→1166，+5 亲和测试）；ruff 全过；引擎金标 29 例随全量绿（多轮拼接 `merge_own_hits` 自动同受益）。
- 实验脚本复算与生产实现 after **逐位一致**（86.2/73.3；重写时曾把 idf 输入改成行级列表引入偏差，已修并有注释钉住「资产级去重」口径）。
- 浏览器 live（客服页，会话 #240）：「保温杯的净含量是多少？」→「净含量为 500ml [1]」引 **A-0009**（75 刀时 top3=[226,227,227] 全 OFF 他品）；「保温杯的材质是什么？」→「材质为钛钢[2]」引 A-0009+A-0003（正确规格资产进窗，75 刀时 top1 被探针对话抢走）。
- 披露：验收在演示库会话 #240 追加了 4 条消息（不破坏其素材性，README 主线不含 #240）。

## 偏差与教训

- 无裁决性偏离。一次自抓：实验脚本重写时 `title_idf` 输入从资产级 dict 变行级列表，同一资产的 title 按 chunk 数重复计入 df/N——**复算与生产逐位对齐是必须的门**（不对齐说明实验证据链断了）。

## 后续

- 下一刀=第 80 刀（顾客侧「结束会话」+ 会话终态语义，Owner 已裁决允许）；审计刀 16 于第 80 刀后。
- 观察位：pos-008/syn-012 两条语义歧义 case 若将来多轮上下文（「该商品」指代解析）立项可回补；混淆组「字段名+怎么样」无实体问句的正确归宿仍是拒答留治理，不追。
