# 第 83 刀 closeout：演示库数据卫生 + 废弃语义落到证据面

日期：2026-09-11。`feat/data-hygiene-83`。**无新表无迁移**。

## 交付

| 件 | 交付 |
| --- | --- |
| 核实（推翻走查判断） | 走查记为「已发布探针资产」的 260-262 实为**标题缺失的真实资料**（「仓库存储位置说明」/「羊绒围巾起球处理」；261/262 逐字重复）；269 是 268 的**劣化副本**（5 块含重复 vs 268 三块）；232「xxx 规格（OFF）」占位资产被 golden 引用 2 次 |
| 数据修复 | 补 3 条空标题（13「退货政策」=golden pos-008/017 期望资产、260、261）；废弃 2 条（262 重复、269 劣化副本）。原则：可逆优先（废弃=0042 标记可回滚；补标题改 title 不动版本=评测零扰动，asset_versions 未动、历史块不与当前指针混用） |
| **顺带揪出真缺陷** | `retrieve`/`oov_verdict` 三处候选 SQL 只过滤 `status='published'`、**没过滤 `discarded_at`**——0042 废弃语义在证据面没落地（此前从无「已发布后废弃」资产故未暴露；本刀 live 实测「羊绒围巾起球怎么办」引用里出现刚废弃的资产）。修 3 处 + 钉子（已发布后废弃不进候选） |
| **MCP 出口同口径**（评审 P1） | `mcp_server.py` 的 `get_asset`（按 id 取全文）与 `export_all_published` 同样漏过滤——demo_reset docstring 明言废弃「列表/检索/导出都不再出现」，导出面 1/3 未兑现。修 2 处（本刀使「已发布后废弃」首次成为现实，故一并补齐） |
| demo_reset | 加「空标题已发布」报告段（**只提示不自动清**——可能是 golden 引用的真实资料）+ 计数排除已废弃 |

## 验收

- 集成 **1191 passed / 0 skipped**（1189→1191，+2）；ruff 全过；`oov_criterion.py` 全部通过（97 判别零误杀保持）。
- live（rebuild 后）：**「羊绒围巾起球怎么办」只引 261（废弃的 262 已出局）**；「退货政策是什么」引 13（当前有效政策）+6；「仓库在哪里」引 260（补标题后实体亲和生效）；demo_reset dry-run `published_without_title: 0`。

## 评测认账（数据动作纪律——数字有变动，如实刷新）

| 指标 | before | after |
| --- | --- | --- |
| positive recall@1 | 87.5% | **90.0%** |
| paraphrase recall@1 | 92.0% | **88.0%** |
| overall recall@1 | 86.2% | 86.2%（持平） |

- positive +1：退货政策资产补标题后亲和生效（pos-008/017 所在域）。
- paraphrase -1：**syn-005 从资产 6 位移到 13**。**订正我 commit 的定性**（Spec 轴指出这是过度乐观）：「13 是当前有效政策、语义更准」的说法不成立——13 的块文本是 6 的超集（「退货政策：签收后7天内…」），位移靠亲和 + 超集文本顶掉，**属横移/噪音**（ADR 0048 已记 6/13 同为政策资产、金标钉 6 属构造）；overall 持平是 +1/-1 相抵，非改善。
- intake 的 Must 3「逐位不变」未达成——本 closeout 如实记录并刷新基线（72 刀先例：数据改善/变动要认账）。

## 两轴评审（实修）

- **Standards P1**：MCP 两个出口漏废弃过滤（上表已修）。P2：`rerank_experiment.py` 镜像 SQL 未同步（已加，防实验/生产口径分叉）；`_can_publish` 可对已废弃资产开修订（发新版检索永不可见——状态机与隐藏标记冲突）、`ops.py fetch_published_refs` 未过滤——**记债**。
- **Spec P1**：syn-005 定性订正（上节）；Must 3 未达成的回写（本 closeout）。
- 钉子判别力：两钉在 origin/main 上红（评审复现）；MCP 出口过滤**无独立钉子**（test_mcp 是单体序贯 harness，加钉需复制整套上传/发布流程）——由评审复核 + 全量回归覆盖，如实披露。

## 记债

- `_can_publish` / `ops.py fetch_published_refs` 的废弃过滤（状态机面）。
- 232 占位资产（品牌 xxx，golden 引用 2 次）不动——OFF 导入数据的占位形态，改它要同步改 golden。
- 18（ingested 空标题、0 chunk）不动（未发布不进检索）。

## 后续

- 走查方案起的**最后一条挂起裁决项**（探针资产处置）本刀收口。
- 下一刀候选：审计刀 16/82/83 结转记债（会话生命周期指标、OOV 点名文案、每 ask 三笔查询合并、`_can_publish` 废弃面、OOV 搭车工单语义）或部署面（用户暂缓）。
