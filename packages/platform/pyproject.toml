[project]
name = "suite-platform"
version = "0.1.0"
description = "中台基础库：对象存储等与业务无关的平台设施"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/suite_platform"]
